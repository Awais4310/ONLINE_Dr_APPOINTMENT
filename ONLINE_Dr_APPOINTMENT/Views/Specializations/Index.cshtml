@model IEnumerable<ONLINE_Dr_APPOINTMENT.Models.Specialization>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Hospital.Hospital_Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Specialization_Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Hospital.Hospital_Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Specialization_Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Specialization_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Specialization_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Specialization_id })
        </td>
    </tr>
}

</table>
